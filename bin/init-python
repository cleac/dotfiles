#!/bin/sh
# vim: set syntax=shell

shopt -s extglob

VIRTUALENV_CMD="virtualenv"
VENV_ARGS='--clear'

for arg in $@; do
   case $arg
      in ( +([0-9])?(.)*([0-9]))
         VENV_ARGS="$VENV_ARGS -p python$arg"
         ;;
   esac
done

VENV_ARGS="$VENV_ARGS env/"

$VIRTUALENV_CMD $VENV_ARGS
